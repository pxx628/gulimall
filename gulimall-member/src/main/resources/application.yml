spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    url: 
    password: 
  application:
    name: gulimall-member
  cloud:
    nacos:
      discovery:
        server-addr: 121.4.30.161:8848

mybatis-plus:
  mapper-locations: classpath:/mapper/**/*.xml
  global-config:
    db-config:
      id-type: auto
server:
  port: 8000
